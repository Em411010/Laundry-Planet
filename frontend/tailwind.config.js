/** @type {import('tailwindcss').Config} */
export default {
  // With Tailwind v4 + @tailwindcss/vite, content is inferred
  plugins: [require('daisyui')],
  daisyui: { themes: ["light", "corporate", "aqua"] }
}
